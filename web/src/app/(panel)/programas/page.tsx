import { Proximamente } from "@/components/ui/proximamente";

export default function ProgramasPage() {
  return (
    <Proximamente
      titulo="Programas"
      descripcion="Crea plantillas base reutilizables y asígnalas a tus clientas."
      sprint="Editor de programas con drag & drop (Sprint 2 — siguiente entrega)."
    />
  );
}
