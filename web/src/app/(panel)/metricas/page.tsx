import { Proximamente } from "@/components/ui/proximamente";

export default function MetricasPage() {
  return (
    <Proximamente
      titulo="Métricas"
      descripcion="Peso, perímetros, composición, pasos y fotos de progreso."
      sprint="Disponible en el Sprint 3."
    />
  );
}
